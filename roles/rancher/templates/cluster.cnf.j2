nodes:
  - address: {{ groups['all'][0] }}
    user: rancher
    role: [controlplane,worker,etcd]
  - address: {{ groups['all'][1] }}
    user: rancher
    role: [controlplane,worker,etcd]
  - address: {{ groups['all'][2] }}
    user: rancher
    role: [controlplane,worker,etcd]

services:
  etcd:
    snapshot: true
    creation: 6h
    retention: 24h
